_target_: experiments.amplitudes.wrappers.AmplitudeTransformerWrapper
mlp_blocks: 1
mlp_channels: ${model.net.out_channels}
average: false

net:
  _target_: experiments.baselines.Transformer

  in_channels: 8
  out_channels: 128
  hidden_channels: 128

  num_blocks: 4
  num_heads: 4

  pos_encoding: false
